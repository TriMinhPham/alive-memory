# Runtime dependencies for the Alive shopkeeper system
#
# Required — system will not start without these
aiosqlite>=0.19.0
anthropic>=0.40.0
colorama>=0.4.6      # terminal + server color output (module-level import)

# Optional — features degrade gracefully if missing
aiohttp>=3.9.0       # ambient weather via wttr.in
feedparser>=6.0.0    # RSS feed ingestion
